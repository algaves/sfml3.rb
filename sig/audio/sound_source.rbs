module SFML
  # The common base of the playable audio classes. Its surface is generated per
  # concrete class from ext/audio/sound_source.inc, so it declares no methods.
  class SoundSource
  end
end
